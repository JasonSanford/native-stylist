export type StylistThemeType = {
  brandColor?: string;
  accentColor?: string;
  fontFamily?: string;
  fontFamilyBold?: string;
};

export enum ButtonVariant {
  Flat,
  Primary,
  Secondary,
  Accent,
  Gray,
  Success,
  Danger,
}
